# Development defaults for running the API with Neon Local via docker-compose.dev.yml
PORT=3000
NODE_ENV=development
LOG_LEVEL=debug

# Application secrets (replace with secure values before running)
ARCJET_KEY=ajkey_01k9cxyb48ekhbyvypzby9fxvh
JWT_SECRET=replace-with-a-dev-secret

# Neon Local proxy configuration
# Your Neon API key must have access to the target project
NEON_API_KEY=napi_x9864xjgtsyn1o01b33zq5evwsvvqc2wow9xmhpbqzfd5cnsv35s66ls0cqpdv42
NEON_PROJECT_ID=wild-poetry-20551369
# Parent branch ID is used to create ephemeral branches automatically
PARENT_BRANCH_ID=br-weathered-sound-a48pndbh
NEON_LOCAL_PROXY_URL=http://neon-local:5432/sql

# Postgres connection string that the Node app/drizzle use inside the compose network
DATABASE_URL=postgres://neon:npg@neon-local:5432/appdb?sslmode=disable
